# Agent rules (IBM Bob, Codex, and other agents)

Follow `CLAUDE.md` in this folder: same lanes, folder ownership, naming rules, no secrets, and the UI gate.
IBM Bob sessions: keep prompts small and file-specific (40 Bobcoins per account), and export every task to `bob_sessions/<name>/<NN-slug>/` (plan/ref/R7-bukti-bob.md).
