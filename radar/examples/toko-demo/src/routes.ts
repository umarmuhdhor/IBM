/**
 * Simple route map used by the app.
 * Experiment task 1 adds '/coupon' and task 5 adds '/orders'.
 *
 * This is an intentional touch-point for the experiment tasks.
 */
export const routes: Record<string, string> = {
  '/': 'Home',
  '/cart': 'Cart',
  '/checkout': 'Checkout',
};
