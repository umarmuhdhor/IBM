/**
 * Coupon logic stub — experiment task 1 will replace this with real logic.
 *
 * @param total  The order total before any discount.
 * @param code   Optional coupon code (ignored for now).
 * @returns      The total unchanged until coupon support is implemented.
 */
export function applyCoupon(total: number, code?: string): number {
  void code; // suppress unused-parameter lint until task 1 is implemented
  return total;
}
