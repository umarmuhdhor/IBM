# Demo script (≤ 3 min video)

> Skeleton from fase 00. Filled in by the owning phase.

## Fase 06 (review & commit)
- Approve di Mission Control → commit GitHub asli atas nama coder + `Co-authored-by: IBM Bob`, hanya file task itu; kartu Selesai menampilkan link commit.
- Diff task menunjukkan `calculateTotal()` berubah + importer `Header.tsx` milik rekan (momen near-miss menit 2:40).
